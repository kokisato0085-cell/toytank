# Co-op の接続先 relay（Cloudflare DO）の WebSocket URL。
# 未設定ならローカル開発用の既定 ws://localhost:8787/ws を使う。
# 本番（GitHub Pages＝https）は wss:// 必須。relay を wrangler deploy した後にそのURLを設定する。
# 例:
# VITE_RELAY_URL=wss://toytank-relay.<account>.workers.dev/ws
