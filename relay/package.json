{
  "name": "toytank-relay",
  "version": "0.0.0",
  "private": true,
  "description": "ToyTank 段階2 Co-op の部屋＝リレーサーバー（Cloudflare Workers + Durable Objects）。WebSocketを中継するだけ（シミュレーションは持たない）。",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.2",
    "wrangler": "^3.78.0"
  }
}
